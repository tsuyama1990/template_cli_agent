[project]
name = "template_cli_agent"
version = "0.1.0"
description = "Template CLI Agent Project"

[ac_cdd]
# Default AI CLI tool. Can be changed to "gh" or strict openai call etc.
ai_tool_command = ["jules", "remote", "new"]

[paths]
src = "src"
tests = "tests"
docs = "documents"
templates = "templates"

[cycles]
phases = ["1. Spec", "2. Contract", "3. Implementation", "4. QA", "5. Refactor"]
